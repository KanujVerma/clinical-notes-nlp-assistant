# Frontend environment variables
# Copy to .env.local for local dev (leave VITE_API_BASE_URL unset — Vite proxies /api)
# Set VITE_API_BASE_URL in Vercel dashboard for production

# Render backend URL (no trailing slash)
# VITE_API_BASE_URL=https://your-app.onrender.com
